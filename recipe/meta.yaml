{% set name = "napari-sam4is" %}
{% set version = "1.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/{{ name | replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 4e14aae25b35114c309a3358a0a44914dd6fc8efa08d0a394f73369a71bdfef2


build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools
  run:
    - magicgui
    - napari >=0.6.0
    - numpy
    - python >={{ python_min }}
    - pytorch
    - qtpy
    - pyyaml
    - scikit-image
    - sam3
    - segment-anything
    - torchvision
    - requests
    - urllib3
    - Pillow
    - platformdirs

test:
  imports:
    - napari_sam4is
  commands:
    - pip list
    - pip check
  requires:
    - pip
    - pyqt
    - python {{ python_min }}

about:
  home: https://pypi.org/project/napari-SAM4IS
  summary: Create annotations for instance segmentation using Segment Anything models
  license: Apache-2.0
  license_file: LICENSE
  dev_url: https://github.com/hiroalchem/napari-SAM4IS

extra:
  recipe-maintainers:
    - hiroalchem
    - jaimergp
